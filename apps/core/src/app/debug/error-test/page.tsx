'use client'

import { ErrorTestComponent } from '@/components/error/ErrorTestComponent'

/**
 * Error Testing Page - Only available in development
 * Navigate to /debug/error-test to access this page
 */
export default function ErrorTestPage() {
  return <ErrorTestComponent />
}